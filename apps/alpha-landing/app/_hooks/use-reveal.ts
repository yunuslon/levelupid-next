'use client'

import { useEffect, useRef } from 'react'

/**
 * Scroll-reveal via IntersectionObserver (not scroll listener).
 * Adds `is-visible` class when element enters viewport.
 * Safe for reduced-motion: .reveal only animates when
 * @media (prefers-reduced-motion: no-preference), so if reduce,
 * element stays fully visible without transition.
 */
export function useReveal<T extends HTMLElement = HTMLDivElement>() {
  const ref = useRef<T>(null)

  useEffect(() => {
    const el = ref.current
    if (!el) return

    if (
      typeof window !== 'undefined' &&
      window.matchMedia('(prefers-reduced-motion: reduce)').matches
    ) {
      el.classList.add('is-visible')
      return
    }

    const observer = new IntersectionObserver(
      (entries) => {
        for (const entry of entries) {
          if (entry.isIntersecting) {
            entry.target.classList.add('is-visible')
            observer.unobserve(entry.target)
          }
        }
      },
      { threshold: 0.15, rootMargin: '0px 0px -8% 0px' },
    )

    observer.observe(el)
    return () => observer.disconnect()
  }, [])

  return ref
}
